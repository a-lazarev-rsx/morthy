import '@testing-library/jest-dom';

// Create a div with id 'root' and append it to the body
const rootElement = document.createElement('div');
rootElement.id = 'root';
document.body.appendChild(rootElement);
